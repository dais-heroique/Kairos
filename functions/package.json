{
  "name": "@kairos/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "serve": "pnpm build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@kairos/core": "workspace:*",
    "@kairos/shared": "workspace:*",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "@kairos/payments": "workspace:*",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
