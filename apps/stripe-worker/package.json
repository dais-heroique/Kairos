{
  "name": "@kairos/stripe-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "cf:build": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @kairos/shared build && pnpm --filter @kairos/payments build"
  },
  "dependencies": {
    "@kairos/payments": "workspace:*",
    "@kairos/shared": "workspace:*",
    "jose": "^5.9.6",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^3.99.0"
  }
}
